const initialstate = {
	updating: false,
	update_success: false,
	update_failed: false,
};
export function updateReducer(state = initialstate, action) {
	switch (action.type) {

	default: return state;
	}
}